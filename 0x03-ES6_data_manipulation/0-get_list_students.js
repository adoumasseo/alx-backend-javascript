export default function getListStudents() {
  const firstObject = { id: 1, firstName: 'Guillaume', location: 'San Francisco' };
  const secondObject = { id: 2, firstName: 'James', location: 'Columbia' };
  const lastObject = { id: 5, firstName: 'Serena', location: 'San Francisco' };

  return [firstObject, secondObject, lastObject];
}
